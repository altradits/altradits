--- DAY 096 SOCRATIC LOG ---

Q1: What is "Sticky Sessions" (Session Affinity)? Why might we need it 
if we weren't using stateless JWTs for our authentication?

Q2: What is the difference between 'Layer 4' (TCP) and 'Layer 7' (HTTP) 
load balancing? Which one allows us to route traffic based on the 
URL path (e.g., /api vs /static)?

Q3: In a cloud environment, who typically handles 'Service Discovery'? 
(Hint: Look up 'Consul' or 'Kubernetes CoreDNS').





# To test this, you would run:
# docker-compose up --scale forge-app=5

git add 05-Architecture/Day-096-scaling/
git commit -m "feat(arch): implement load balancing with Nginx and Docker service scaling"