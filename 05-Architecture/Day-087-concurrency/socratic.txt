--- DAY 087 SOCRATIC LOG ---

Q1: If each 'AuditTransaction' takes 500ms, and we audit 10 
transactions, why does the program finish in ~500ms instead of 5 seconds?

Q2: What happens if you forget to call 'wg.Add(1)' inside the loop? 
(Hint: Will the main function wait?)

Q3: What happens if you forget to call 'wg.Done()'? 
(Hint: Look up "Deadlock").




# Observe the speed difference
go run 05-Architecture/Day-087-concurrency/main.go

git add 05-Architecture/Day-087-concurrency/
git commit -m "feat(arch): implement parallel audit processing using goroutines and waitgroups"