--- DAY 074 SOCRATIC LOG ---

Q1: Why is it dangerous to only have "Up" migrations? Why must we 
always write a "Down" migration for every schema change?

Q2: How does the migration tool know which version the database is 
currently on? (Research: The 'schema_migrations' table).

Q3: In a CI/CD pipeline, should the Go application run the 
migrations automatically on startup, or should a separate 
orchestration tool handle it?



# Install the migration tool
# brew install golang-migrate (Mac) 
# or use the Docker version

# Run migrations
migrate -path migrations -database "postgres://altradits_admin:forge_password@localhost:5432/altradits_vault?sslmode=disable" up

git add 03-Persistence/Day-074-migrations/
git commit -m "feat(persistence): implement versioned migrations and data seeder"