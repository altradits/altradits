--- DAY 073 SOCRATIC LOG ---

Q1: Why do we use 'pgxpool' instead of the standard 'database/sql' 
library? What PostgreSQL-specific features does 'pgx' expose?

Q2: What happens if 'MaxConns' is set to 1, but 50 Founders try to 
load the dashboard at the same time? How does the pool manage the 49 
waiting users?

Q3: Why is 'defer pool.Close()' essential in the main function? 
What happens to the PostgreSQL server if we "ghost" its connections?