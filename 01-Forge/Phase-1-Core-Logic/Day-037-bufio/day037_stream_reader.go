package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	// TASK:
	// 1. Create a large text file manually or via code (10+ lines).
	// 2. Open the file using os.Open.
	// 3. Use bufio.NewScanner to read the file line-by-line.
	// 4. Print each line with a prefix: "LOG_ENTRY -> [line content]".
	// 5. Challenge: Count the total number of words in the file using bufio.Scanner.
}