package main

import (
	"errors"
	"fmt"
)

// TASK:
// 1. Create a function 'Withdraw(balance int, amount int) (int, error)'.
// 2. If amount > balance, return 0 and a new error: "insufficient funds".
// 3. In main, call Withdraw with an amount larger than the balance.
// 4. Use 'if err != nil' to catch the error and print it.
// 5. Challenge: Use errors.New() and fmt.Errorf() to see the difference in formatting.

func main() {
    // Execution goes here...
}
