# Anomaly Report: Day 012

## The Anomaly to Test
**Overlapping Counts:** Does `strings.Count` handle overlapping patterns (e.g., counting "banana" in "banananana")?

## Execution Steps
1. Count the occurrences of "aba" in "ababa".
2. Check the documentation to see if it counts overlapping or non-overlapping instances.

## The Fintech Lesson
If we are counting "Double Zero" patterns for a specific currency logic, we must know if the library counts them as separate pairs or as a single sequence.