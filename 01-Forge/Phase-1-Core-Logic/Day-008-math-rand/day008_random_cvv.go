package main

import (
	"fmt"
	"math/rand"
	"time"
)

func main() {
	// TASK:
	// 1. Seed the random generator using the current time: rand.Seed(time.Now().UnixNano()).
	// 2. Generate a random 3-digit CVV code (100-999).
	// 3. Generate a random "Transaction ID" between 100,000 and 999,999.
	// 4. Output: "Security Check: Your CVV is [val] and TxID is [val]."
}
