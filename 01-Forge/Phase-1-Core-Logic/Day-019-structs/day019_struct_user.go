package main

import "fmt"

// TASK:
// 1. Define a struct named 'Account' with fields: ID (string), Owner (string), Balance (int64), and IsActive (bool).
// 2. In main, create an instance of 'Account' using a struct literal.
// 3. Print the account details using dot notation (e.g., myAcc.Owner).
// 4. Update the Balance field and print the updated struct.
// 5. Challenge: Create an anonymous struct for a "One-Time Transaction" log.

type Account struct {
	ID       string
	Owner    string
	Balance  int64
	IsActive bool
}

func main() {
    // Execution goes here...
}
