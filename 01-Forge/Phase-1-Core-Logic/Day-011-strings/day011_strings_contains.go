package day011strings
package main

import (
	"fmt"
	"strings"
)

func main() {
	// TASK:
	// 1. Declare a variable 'ledgerEntry' with a string (e.g., "TXN_9827:DEPOSIT:SUCCESS").
	// 2. Use strings.Contains to check if the entry contains the word "SUCCESS".
	// 3. Use strings.HasPrefix to check if the entry starts with "TXN".
	// 4. Output a boolean verification for both checks.
	// 5. Challenge: Check if a user's email input contains both "@" and ".".
}
