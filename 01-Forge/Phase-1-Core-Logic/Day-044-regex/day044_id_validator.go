package main

import (
	"fmt"
	"regexp"
)

func main() {
	// TASK:
	// 1. Create a regex pattern for an Altradits ID: 2 letters followed by 4 numbers (e.g., "TX1234").
	// 2. Use regexp.MatchString to validate a few test IDs.
	// 3. Use regexp.Compile to create a 'Regexp' object (for performance).
	// 4. Use 'FindString' to extract an ID out of a messy sentence.
	// 5. Challenge: Validate that a string looks like a simple email address.
}