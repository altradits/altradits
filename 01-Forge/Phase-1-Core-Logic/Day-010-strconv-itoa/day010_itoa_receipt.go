package main

import (
	"fmt"
	"strconv"
)

func main() {
	// TASK:
	// 1. Declare an integer 'transactionID' as 987654.
	// 2. Use strconv.Itoa to convert it to a string.
	// 3. Concatenate (combine) it with a prefix: "TXN_" + strID.
	// 4. Output the final "Receipt ID" string.
	// 5. Challenge: Use strconv.FormatInt to convert a balance to a Hexadecimal string.
}
