package main

import (
	"fmt"
	"sort"
)

func main() {
	// TASK:
	// 1. Create a slice of integers representing transaction amounts: [500, 10, 250, 100, 5].
	// 2. Use sort.Ints to sort them in ascending order.
	// 3. Print the sorted list.
	// 4. Use sort.Slice to sort a list of strings ["Z", "A", "M"] in descending order.
	// 5. Challenge: Sort a slice of 'Account' structs by their Balance field.
}