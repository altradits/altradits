package day014stringssplit
package main

import (
	"fmt"
	"strings"
)

func main() {
	// TASK:
	// 1. Declare 'csvData' as "Stan,100,Deposit,Success".
	// 2. Use strings.Split to turn this into a slice of strings.
	// 3. Print the 2nd item (the amount) and the 4th item (the status).
	// 4. Use strings.Join to turn the slice back into a string, but using ":" instead of ",".
	// 5. Output: "Stan:100:Deposit:Success".
}
