package main

import (
	"fmt"
	"strings"
)

func main() {
	// TASK:
	// 1. Declare 'rawInput' as "Balance: $1,000.00".
	// 2. Use strings.Replace to remove the "$" symbol.
	// 3. Use strings.ReplaceAll to remove the "," (comma).
	// 4. Use strings.TrimSpace to clean any accidental leading/trailing spaces.
	// 5. Output the cleaned string: "1000.00".
}
