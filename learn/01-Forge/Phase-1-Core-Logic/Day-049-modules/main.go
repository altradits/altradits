package main

import (
	"fmt"
	"github.com/google/uuid"
)

func main() {
	// TASK:
	// 1. Initialize a new module: 'go mod init altradits/core'.
	// 2. Import an external package (e.g., github.com/google/uuid).
	// 3. Run 'go mod tidy' to fetch the dependency.
	// 4. Generate a new UUID for a transaction and print it.
	// 5. Observe: How does the 'go.sum' file change after running the code?
}