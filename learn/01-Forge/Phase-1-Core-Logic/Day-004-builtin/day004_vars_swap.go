package main

func main() {
	// TASK:
	// 1. Declare two variables: 'senderBalance' and 'receiverBalance' using short declaration (:=).
	// 2. Assign them initial values (in cents).
	// 3. Perform a "Manual Swap": Move the sender's balance to the receiver and vice versa.
	// 4. Use a temporary third variable to prevent data loss during the swap.
	// 5. Challenge: Research and implement the Go "Tuple Assignment" swap: a, b = b, a.
}
