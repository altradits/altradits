package main

import "fmt"

func main() {
	// TASK:
	// 1. Declare an integer 'vaultBalance' = 1000.
	// 2. Create a pointer 'ptr' that points to the memory address of 'vaultBalance'.
	// 3. Print the value of 'vaultBalance', the address stored in 'ptr', and the dereferenced value (*ptr).
	// 4. Update the balance by modifying the value through the pointer (*ptr = 2000).
	// 5. Challenge: Pass the pointer to a function 'AuditVault(p *int)' that increments the value by 1.
}
