package main

import (
	"fmt"
	"time"
)

// TASK:
// 1. Create a function 'AuditAccount(id string)' that prints "Starting Audit for [id]"
//    sleeps for 500ms, and then prints "Audit Complete for [id]".
// 2. In main, call this function twice using the 'go' keyword (goroutines).
// 3. Add a time.Sleep(2 * time.Second) at the end of main.
// 4. Observe: Do the audits run one after another, or at the same time?
// 5. Challenge: What happens if you remove the time.Sleep in main?

func AuditAccount(id string) {
    // Logic here...
}

func main() {
    // Execution here...
}