package main

func main() {
	// TASK:
	// 1. Declare a variable 'depositAmount' (int).
	// 2. Use fmt.Scanln to capture user input from the terminal.
	// 3. Print a confirmation: "Processing deposit of [value] cents to Altradits Core..."
	// 4. Challenge: Capture two separate inputs (Account ID and Amount) in one session.
}
