package main

import (
	"fmt"
	"sync"
	"time"
)

// TASK:
// 1. Initialize a sync.WaitGroup.
// 2. Launch 3 goroutines, each representing a "Bank Branch Audit".
// 3. Use wg.Add(1) before starting each goroutine and wg.Done() inside each.
// 4. Use wg.Wait() in main to ensure all audits finish before the program prints "All Branches Secure".

func main() {
    // Execution goes here...
}