package main

import (
	"fmt"
	"strconv"
)

func main() {
	// TASK:
	// 1. Declare 'inputAmount' as a string (e.g., "500").
	// 2. Use strconv.Atoi to convert the string to an integer.
	// 3. Handle the error: If conversion fails, print "Invalid Transaction Format".
	// 4. If successful, add 100 "bonus" cents to the value and print the total.
	// 5. Challenge: Capture a string from the user using fmt.Scan and then convert it.
}
