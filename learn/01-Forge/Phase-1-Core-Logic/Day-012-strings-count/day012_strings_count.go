package main

import (
	"fmt"
	"strings"
)

func main() {
	// TASK:
	// 1. Declare a long string 'auditLog' containing several "ERROR" and "WARN" tags.
	// 2. Use strings.Count to find exactly how many errors occurred.
	// 3. Use strings.ToLower to normalize a user's command input (e.g., "EXIT" to "exit").
	// 4. Output the count and the normalized string.
}
