--- DAY 085 SOCRATIC LOG ---

Q1: If SQLc uses prepared statements, why is it still dangerous 
to use 'fmt.Sprintf' to build an ORDER BY clause? 
(Hint: Can placeholders be used for table or column names?)

Q2: What is the "Content Security Policy" (CSP) header, and how 
does it act as a second line of defense against XSS if a 
developer accidentally uses 'template.HTML'?

Q3: In the Altradits Forge, if a Founder uploads a document, 
why should we serve it with 'Content-Type: application/octet-stream' 
instead of letting the browser "sniff" the file type?